# URL do backend sem barra no final
# Em desenvolvimento: deixar vazio (usa o proxy do Vite → localhost:5000)
# Em produção (Azure): URL do App Service
VITE_API_URL=https://sistema-traction-api.azurewebsites.net
